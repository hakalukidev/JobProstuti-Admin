# TODO

- [ ] app/login/page.tsx এ login success হলে `isLoggedIn=true` cookie সেট করা
- [ ] login success হলে `router.push('/')` করা (dashboard: `app/page.tsx`)
- [ ] (ঐচ্ছিক) logout না থাকলে শুধু login flow যথেষ্ট কিনা যাচাই
- [ ] অ্যাপ চালিয়ে login করে নিশ্চিত করা যে middleware redirect করে ড্যাশবোর্ডে নেয়

