'use client';

import SectionShell from '@/components/SectionShell';

export default function Users() {
  return (
    <SectionShell
      title="Users"
      description="User profiles, roles, and lifecycle management-এর জন্য a refined admin workspace with the same dark-green language."
    />
  );
}
