'use client';

import SectionShell from '@/components/SectionShell';

export default function Subscriptions() {
  return (
    <SectionShell
      title="Subscriptions"
      description="Subscription plans, renewals, and user billing visibility-এর জন্য a clean finance-friendly dashboard view."
    />
  );
}
