'use client';

import SectionShell from '@/components/SectionShell';

export default function Settings() {
  return (
    <SectionShell
      title="Settings"
      description="System preferences, access rules, এবং admin controls-এর জন্য a polished configuration center."
    />
  );
}
