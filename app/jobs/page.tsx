'use client';

import SectionShell from '@/components/SectionShell';

export default function Jobs() {
  return (
    <SectionShell
      title="Jobs"
      description="Job postings, moderation, এবং opportunity listings পরিচালনার জন্য একটি polished operations view."
    />
  );
}
