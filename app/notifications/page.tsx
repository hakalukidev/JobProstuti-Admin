'use client';

import SectionShell from '@/components/SectionShell';

export default function Notifications() {
  return (
    <SectionShell
      title="Notifications"
      description="User alerts, admin notices, এবং system updates এক জায়গায় manage করার জন্য একটি focused control panel."
    />
  );
}
